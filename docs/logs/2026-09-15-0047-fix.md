# 2026-09-15 任务日志：修复 0047 Storage 迁移权限错误

- 日期：2026-09-15
- 角色：后端工程师 / 运维
- 任务：处理执行 `0047_textbook_storage.sql` 时 `must be owner of table objects` 错误。
- 完成内容：移除对 Supabase 系统表 `storage.objects` 的 `COMMENT` 操作；保留教材私有桶、对象归属函数和 4 条按账号隔离的 Storage RLS 策略。迁移中的 `insert`、`create or replace function`、`drop policy if exists` 和策略创建均保持幂等。
- 修改的文件：
  - `supabase/migrations/0047_textbook_storage.sql`
  - `docs/logs/2026-09-15-0047-fix.md`
- 验证：已确认报错只来自迁移末尾的 `comment on table storage.objects`；未连接目标 Supabase 执行线上 SQL。
- 遗留问题：需要在目标 Supabase 重新执行修正版 0047，并运行验证 SQL；当前环境无法替用户确认线上桶状态。
- 下一步：运维/项目负责人重新整段执行 0047；确认 `textbooks` 桶为私有并存在 4 条 `textbooks_*` 策略后再测试教材上传。
