# 2026-09-15 任务日志：将 0047 精简为桶创建 SQL

- 日期：2026-09-15
- 角色：后端工程师 / 运维
- 任务：避免 SQL Editor 对 Supabase `storage.objects` 系统表的间接触碰。
- 完成内容：将 0047 精简为幂等创建私有 `textbooks` 桶的单条 SQL；Storage 策略全部改为在控制台 Policies 页面创建，使用 `split_part` 按对象路径校验当前用户，不依赖数据库辅助函数。
- 修改的文件：
  - `supabase/migrations/0047_textbook_storage.sql`
  - `docs/0047_textbook_storage_policies.md`
  - `docs/logs/2026-09-15-0047-minimal-sql.md`
- 验证：本地确认 0047 不再包含 `storage.objects`、`create policy`、`drop policy` 或函数授权语句。
- 遗留问题：需要在 Supabase 控制台创建 4 条 `textbooks_*` 策略；旧 SQL Editor 标签页可能仍保留旧脚本。
- 下一步：新建 SQL 查询，只执行 0047 当前单条桶创建语句；再按策略文档完成控制台配置并验证上传。
