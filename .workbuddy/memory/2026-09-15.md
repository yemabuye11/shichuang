# 2026-09-15 工作日志

## 组卷（exam paper assembly）功能方向讨论
- 用户提议：参考网上云南/各地区试卷、学科网（用户有账号密码，愿提供）来命题；要求"知识点相同、题不一样"的变式生成；原件下载也要积分。
- 指出最大困难=版权合规：学科网内容有版权、条款禁自动抓取/生成竞争内容；师创商业化下"改词生成再卖"属演绎作品有追责风险；自动登录还违反条款+我不应经手用户密码。工程上学科网反爬/验证码脆弱，成本上原件下载消耗用户积分不可控。
- 提出三条合规路径：A) 老师上传自有/学校卷（复用 T10 上传+AI识别）；B) 手动贴例题（非爬）；C) 纯知识点生成（最干净）。不建议自动抓学科网。
- 与 T10 边界：每日一练=日常客观题；组卷=正式测验卷（大题/分值/答题卡），建议独立模块复用基础设施。
- 状态：规划阶段，已向用户呈困难分析+合规方案，待用户确认题源方式与模块形态后再实现。

## 组卷地基已完成并推送（T11）
- `supabase/migrations/0040a_enum_exam_paper.sql`：`app_type_enum` 加 `'exam_paper'`（**必须单独先跑**，避免 55P04）。
- `supabase/migrations/0040_exam_paper.sql`：`exam_papers` / `exam_paper_questions`（含 section_no/section_title/score/knowledge_point，qtype 含 subjective）/ `exam_paper_submissions`（subjective_scores jsonb）+ RLS + 5 个 RPC。客观题判分**复用 0038 的** `practice_grade_question` / `practice_answer_text`（表无关通用函数）。
- `supabase/functions/exam-paper/index.ts`：action = parse(免费) / generate / import，积分 reserve→settle→refund，LLM 走 chooseAdapter，输出 `parsePaper()` 校验。45/45 Edge 语法体检通过。
- `.github/workflows/deploy.yml` 加 `supabase functions deploy exam-paper`。
- commit：`a160563`（数据层）、`7ae3ab8`（Edge+部署）。

## 用户拍板的两个决策
- **创作方式**：题库优先（方案3 题库为底座），同时保留①填规格快速生成、②上传原卷（解析→自动沉淀进题库）。每次生成/上传的题目**自动沉淀进题库**，老师可回来勾选组卷。
- **学科网用法（合规姿势）**：用户手动下载（自有账号、免费）→ 手动粘贴/上传进师创 → AI 提炼成「知识点 + 全新题目」入自有题库。**原卷不进库、不转卖**；**不做**自动登录爬取（违反条款+不能代管密码）；"免费下载"≠"可用于商业竞品库"。此路对应既有 BR-014/015「内容沉淀降本」策略。

## 客户新一批需求（四大块，本轮已派工）
一、板块重组：①首页仅保留生成类功能，一句话/教案/课件移到首页第一板块；②每日一练不在首页、不在生成页，改独立页面（**需回改：我之前在 GeneratePage 加过每日一练入口 Card，要删**）；③新增独立组卷板块与每日一练并列；④删除「批量生成兑换码」板块；⑤题库搭建要花心思（现有模板质量差）。
二、Bug：管理后台「注册用户」页与「代充值」页均报 `column reference id is ambiguous`（联表未给 id 指定表别名）。
三、质量：①大幅提升课件生成质量至可直接对外展示；②真正实现一句话生成教案/课件；③重新评估积分消耗（当前积分不足以支撑高质量生成）。
四、功能：实现微信收款图片链接 + 给客户可操作的配置方案（客户不会操作）。

## 并行派工（本轮 4 路，避免同文件冲突）
- `software-engineer`（原 T01–T03，懂数据层/RPC）→ 修 id 歧义 Bug。
- `software-engineer-5`（原主页改版，懂 HomePage）→ 板块重组：HomePage/GeneratePage/TopNav/MobileTabBar；**不碰 AdminPage、不建组卷页**。
- `software-engineer-2`（原前端页面）→ 组卷收尾 + 题库：0041 迁移（knowledge_points + question_bank + RLS + 4 RPC）、Edge 沉淀入题库、types/service、4 个页面、路由导航；**不碰 AdminPage**。
- `software-product-manager` → 只做分析不改代码：`docs/CREDIT_AND_QUALITY_REVIEW.md`（积分量化诊断+修订建议+降本替代方案+课件验收清单+待拍板问题）。

## 约定（避免多人改同一文件打架）
- **AdminPage 单一 owner**：删批量兑换码 + 加组卷配置面板 + 加微信收款码面板，统一放到下一波由一个工程师改。

## 客户最新指示：3D 搁置（2026-09-15）
- 原话「3D的先放一放，我没积分了，消耗积分太猛了」→ **撤回此前「3D 直接根治(3-5天)」的决定，3D 一律不投入**。
- 连带：3D 积分**维持 8 不动**（评审原建议 8→10，不执行）。3D 质量未达标前不建议收费，0044 里留了「可选下架 3D」的注释块待客户拍板。
- 「我没积分了」= 站长本人试用把积分耗光 → 出 0045 补积分应急通道。

## 迁移 0044_credit_cost_quality_v2.sql（commit ef3bc3d，已推送）
- 积分重定价：教案4→6 / PPT6→8 / 2D6→8 / 办公4不动 / **3D 8不动**；应用类 auto2→4、AI教案·大单元2→6、AI组题2→4、互动课件4→5、教学游戏6→7；教学动画6/教育应用4/数据回收4/AI命题2 评审未提，不动。
- 不在此范围：daily_practice、exam_paper（各有 system_config 的 practice/exam 计价表）。
- **月度阀真相（重要，纠偏）**：`monthlySpendCny` 早在 **0025 就已从 100 抬到 1000**，不是评审文档里写的 100。客户口头选的「调到 500」反而把停服阈值砍半 → **按"避免停服"本意保持 1000**，0044 只做 `>= 1000` 兜底，注释里写明要 500 改一处即可。
- **admin_adjust_credits 改 numeric（真 Bug）**：0009/0042 的版本第二参是 integer，调 apply_credit 时**精确匹配到 integer 重载**（0008 定义、不写 credit_lots），导致后台代充值加的积分**没有批次记录**，FIFO 扣减时无 lot 可扣；且返回余额被截成整数。0039 后余额已是 numeric(10,2) → drop integer 重载 + 建 numeric 重载。**0044 必须在 0042 之后跑**（否则 0042 把 integer 版建回来）。

## 迁移 0045_owner_credit_grant.sql（commit ef3bc3d，已推送）
- 站长补积分应急通道：改 v_email（登录邮箱）+ v_amount（默认 2000）→ SQL Editor 跑一次。ref_id 固定 `grant:2026-09-owner`，幂等，再补就换 ref_id。
- 不走 admin_adjust_credits（SQL Editor 里 auth.uid() 为 null 会撞「仅管理员」），直接调 security definer 的 apply_credit(numeric)。
- 日常补积分走管理后台「代充值」Tab（0042 已修好）。

## 前端兜底积分值同步（commit 0e03bc0）
- `src/config/constants.ts` 的 APP_TYPES/DOC_TYPES.creditCost + `supabase/functions/_shared/doc/types.ts` 的 DOC_TYPE_COST（**注意不在 cost.ts**，cost.ts 全是人民币成本记账与月度阀）。
- 别动：`generate/index.ts` 的 `?? 1`、`exam-paper/index.ts` 的 `?? 2`、`practice/index.ts` 的 `?? 1`（读库失败通用回落，非单价）；`creditRules.ts` 的 `CNY_PER_CREDIT=0.05`（内部对账，与实际 ¥0.019/积分 差距大，可能是下一个要对齐的点）；`MONTHLY_SPEND_CAP_CNY=100`（兜底值，0044 只约束 system_config 里的值）。

## 关键根因发现（software-engineer-12，commit e3edc6f）
- 课件「简陋」的真凶不是钱：**0024 的 PPT/2D 提示词自己写着「image 的 src 留空，注明建议配图」**，等于让 AI 不许配图；且 0043 前 doc_output_format 漏了 chart 块类型（但 chart 的 SVG 渲染代码早就有：types.ts L69 / render.ts L186·L226 / exportMap.ts L53）。
- 0043 已把 chart 加回 doc_output_format；0046 继续把 0024/0030 的 PPT/2D 提示词改成「必须出图」口径（每章至少一图或一图表、禁止外链图、允许内联 SVG/data URI、补页数下限）。

## 商业红线提醒（差点踩坑）
- 客户要求「删除批量生成兑换码板块」，但**兑换码是线下人工充值回收成本的唯一通道**，删掉创建入口 = 堵死收款。已拦下，改为在 AdminPage 保留一个精简「兑换码」Tab（数量默认 1、上限 50，不做 2000 批量），由 software-engineer-11 做 CodePanel.tsx。

## 环境
- rollup 原生包又坏过一次（`@rollup/rollup-win32-x64-msvc` 的 .node 报「不是有效的 Win32 应用程序」）。修法：`node -e` 用 `fs.rmSync` 删掉 `node_modules/@rollup/rollup-win32-x64-{msvc,gnu}` 两个目录（**不能用 rm，会被 shim 拦截**），再 `npm install --no-audit --no-fund`（37s，added 2 packages）。
- github.com:443 偶发连不上，push 失败**重试一次即可成功**（不要改 remote）。

## 本轮收尾（全部已推送 origin/main）
- 组卷+题库完成（software-engineer-2）：0041 迁移、exam-paper Edge 沉淀入题库、4 页面、路由导航；commit 1674e3f/8348201/45586dd/d654aa3/d732d87。三条出题路径（填规格/上传原卷变式/题库勾选）、学生免登录 /e/:slug、主观题逐人逐题打分、导出 CSV。
- 兑换码精简 Tab（software-engineer-11）：`src/components/admin/CodePanel.tsx` + AdminPage Tab 索引 8（内容类型积分顺延 9），数量下拉 1/5/10/20/50 默认 1、面值、备注、有效期，生成后明文列出可复制，列表支持筛选/作废。commit 8177d6c。
- 0046 学科分档（software-engineer-12）：commit ee95b90。数据型学科（数/物/化/生/地/信息）每页必须带图、12页至少10页带图、禁连续3页纯文字；文科档（语/史/政/德法/英）每2页1张+整份≥3张+禁0张，可用文章脉络图/时间轴/人物关系图；**禁止为凑数画无信息量装饰图**；跨科兜底=无图页必须用 table/list 结构化，不许留白。
- 0046 上一版（186b27c）SQL 注释里有 3 个 U+FFFD 损坏字符（"只允许内联"写坏），已在 ee95b90 修复并全文件扫描确认 0 个。**教训：写中文 SQL 后应扫一遍 U+FFFD**。
- `prompt_templates` 表**没有 value 列**，内容在 `content` 列，查时要带 `and is_active`（同 key 有多个历史版本行）。0030 不覆盖 doc_type 提示词（只动模型供应商），权威版本链：0016→0024→0026→0043→0046。
- 给客户的执行清单：`docs/本次要跑的SQL清单.md`（顺序表 + 3 段验证 SQL + 报错对照表）。
- 提醒客户：改提示词只影响新生成内容；Edge 有 60 秒缓存；跑完要退出重登。
