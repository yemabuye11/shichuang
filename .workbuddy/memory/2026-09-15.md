# 2026-09-15

## 📋 新功能立项：能力提升练习（链接式），已出大纲待野马拍板
- 需求来源野马（2026-09-15 00:30 左右）：做一个"作业布置"模板模块——熟悉新教材、教师自定义生成天数、链接发给学生做题、教师看完成情况并反馈；积分"一次 1 积分、多天批量打折（10天 6.8 积分）但不能亏"；**合规上不能叫"布置作业"，改叫"提升"**。要求**先出大纲一起拍板再动手**。
- 产出：`docs/PLAN_能力提升练习.md`（已 present_files 展示）。含：合规改名对照表、5 步引导向导、学生端/教师端、积分阶梯（1天1分/3天2.7/5天4.2/10天6.8/20天12，**折扣下限6折**）、成本测算、技术方案（新建 practice_sets / practice_submissions / practice_feedback + 提交 Edge + `/p/` 静态托管路径）、P0/P1/P2 分期、风险表、6 个待拍板问题。
- **成本结论（关键技术判断）**：批量多天生成敢打折的依据是——一次生成 N 天时教材上下文/系统提示词/章节知识只算一次且缓存命中，10 天成本约为单天的 3~4 倍（谷段 ¥0.06~0.10），收入 6.8×¥0.05=¥0.34 → 毛利 50%~80%，**不会亏**。单天 1 积分谷段毛利 60%、高峰 20%，同样不亏。折扣参数必须进 system_config，不写死。
- 待野马回复 6 个拍板项后才开工（命名 / 定价表 / 是否只做客观题 / 学生免登录 / 反馈放 P0 还是 P1 / 首批学段学科）。

## ⚙️ 新增长期协作约定：限流不停工（野马明确要求）
- 原话："如果遇到限流，请设定任务，限流结束后继续跑任务，以后都这样。"
- 已写入 `.workbuddy/memory/MEMORY.md` 协作偏好段：遇 429/限流 → ① 把剩余工作写成明确任务（记录进度与剩哪些步骤） ② 用 `automation_update` 建**一次性定时任务**在限流窗口后自动续跑 ③ 主动告知用户"已排好 X 点自动续跑"，不干等、不推给用户重试。
- 另写入：**新功能（尤其涉及合规/定价）先出大纲给野马拍板再动手**。

## ✅ T10「每日一练」已拍板并基本开发完成（2026-09-15 凌晨~02:30）
- 野马拍板：命名「每日一练」、记录可下载（导出进 P0）、折扣下限 6 折、P0 只做客观题（选择+填空为主）、学生免登录、首批二年级语文；**新增老师上传题目入口（模板可下载 + AI 识别）**，模板规整时本地解析不调模型（省 0.5 积分）。
- 大纲改定：`docs/PLAN_每日一练.md`（旧 `PLAN_能力提升练习.md` 已删）。
- **8 个提交本地完成（待 push）**：
  - `4f408b3` SQL `0038_practice_daily.sql`（practices/practice_questions/practice_submissions 三表 + RLS + 4 个服务端判分/统计 RPC；填空题多空判分口径=单元素数组 `|` 分隔）
  - `840c257` Edge `practice/index.ts`（generate/import 双路径 + 积分结算，语法体检 44/44）
  - `653cac3` 前端 `practiceService`/`types/practice`/`utils/csv`（含 BOM 模板下载、CSV 解析）
  - `0b88dac` 学生端 `PracticeRunPage`(/p/:slug 免登录)
  - `60b4c46` 教师创建 `PracticeCreatePage`（AI 生成 + 上传识别双路径 + 预览编辑）
  - `1230748` 教师看板 `PracticeManagePage`（完成名单/正确率/导出/关闭/清空）
  - `66a5e20` SQL `0039_credit_decimal.sql`（积分账户/流水改 numeric(10,2)，支持 2.7/4.2/6.8 小数积分——原 integer 会截断 2.7→3）
  - `40b0efd` 接线：routes/router/TopNav/MobileTabBar/GeneratePage(入口卡)/MePage/AdminPage(每日一练配置 Tab) + 新建 `PracticeSettingsPanel`
- **野马要手动做的（代码不碰 DB）**：① Supabase SQL Editor 跑 `0038` + `0039`（幂等，重跑安全）；② 推送后 Actions 自动部署 `practice` Edge（已加进 deploy.yml，需仓库已配 `SUPABASE_ACCESS_TOKEN`/`SUPABASE_PROJECT_REF`）；③ 后台「每日一练配置」调折扣/识别积分、「内容类型积分」能看到 daily_practice 行并改价。
- ⚠️ 已知风险点：① `savePractice` 插 practice_questions 的 RLS 靠"父 practices.owner_id=auth.uid()"子查询判权，需真机验证插入不被 RLS 拦；② 上传路径未收集学科/年级（savePractice 填默认值 语文/二年级），若需让上传题也带学科可加 2~3 个 TextField；③ 填空学生端只收单个输入框，多空题老师宜按单空设计。
- ⚠️ 限流管控：本段全程未触发限流；若将来触发，按 `automation_update` 建定时续跑（野马已拍板）。
