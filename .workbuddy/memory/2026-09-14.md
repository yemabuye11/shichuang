# 2026-09-14 工作日志

## T10「每日一练」开发收尾 + 枚举事务冲突修复

- 0038 在 Supabase SQL Editor 报 `55P04: unsafe use of new value "daily_practice"`：同一脚本里先 `alter type app_type_enum add value` 又在同事务 `insert into app_type_profiles` 使用该值 → PG 要求枚举值先提交。
- 修复：把枚举加值块拆成独立文件 `supabase/migrations/0038a_enum_daily_practice.sql`（仅 `do $$ alter type ... add value if not exists ... $$` + 幂等兜底）；原 `0038_practice_daily.sql` 顶部枚举块删除，改为前置依赖说明。
- 运行顺序（写入 `docs/PLAN_每日一练.md` 第六节）：**先 0038a → 再 0038 → 最后 0039**。三者均幂等可重跑。
- 提交：`d203994`（拆分迁移）+ `ab0cdde`（计划文档补运行顺序），均已 push 到 `yemabuye11/shichuang` main。

## 待野马执行 / 验证
- 在 Supabase SQL Editor 按 0038a → 0038 → 0039 顺序粘贴运行；0038 修好后即可跑通。
- 真机验收（PLAN §11）：未提交前接口无 answer、3 天扣 2.7 积分、导出 CSV 中文不乱码、`savePractice` 写 practice_questions 的 RLS 子查询判权。
- `practice` Edge 经 `.github/workflows/deploy.yml` 自动部署，需仓库已配 SUPABASE_ACCESS_TOKEN / SUPABASE_PROJECT_REF。
