# 2026-09-12

## 启动「师创」项目：教师 AI 教学应用生成平台（复刻飞象老师）

用本机 Edge + playwright-core 实地调研了 feixianglaoshi.com（agent-browser 的 Chromium 下载在国内超时，改用系统 Edge 驱动，可行）。

### 客户已拍板的决策
- 复刻范围：整个平台（AI 生成器 + 应用广场社区），AI 是重中之重
- 端形态：网页 + PWA 先行（零成本、发链接即用），架构预留原生 APP / 小程序
- 数据存储：云端多端同步（Supabase 免费层）
- 平台名：**师创**（代号 shichuang）
- 登录：**邮箱 + 密码 + 邀请码**（零成本零资质；不做手机短信、不做微信）
- 大模型：多家适配器，客户自己申请 Key、自己充值
- 商业模式：**会员/套餐 + 积分，线下人工充值，不做在线支付** → 兑换码与管理员后台是核心
- 部署：先用免费海外跑通，成功后迁国内；国内方案（阿里云 OSS+CDN / 腾讯云 COS+CDN）要提前备好迁移清单
- 积分定价暂缓：客户要先看清「做一次多少钱」再定价 → 成本统计升为首要功能

### 团队产出
- 许清楚 PRD：docs/PRD.md（560 行，P0 需求池 A~G、提示词工程专项、6 张 UI 线框、12 个待确认问题）
- 高见远架构：docs/ARCHITECTURE.md（1100 行，5 个任务约 130 文件）+ 2 张时序图 + 类图
  - 关键决策①：应用 HTML 永不过 Supabase 出网（免费层仅 10GB），走静态托管 + CDN，路径 /a/{年}/{月}/{id}/v1.html
  - 关键决策②：积分解耦 + 预扣退还用 Postgres 行锁 + 唯一索引幂等
  - 关键决策③：提示词存表分节版本化，system 固定前缀逐字节稳定以命中上下文缓存
- 寇豆码编码：T01/T02/T03 完成（基础设施、11 个迁移 + RLS + RPC、3 个 Edge Functions、4 家模型适配器 + mock、提示词 seed 16KB）；T04/T05 页面**仍是 18~33 行占位页**时遭遇 429 限流中断（运行 199 分钟）
- 第二轮工程师（software-engineer-2）已启动补 T04/T05 页面，并要求实现**演示模式**：无 Supabase 配置、无 API Key 时也能完整走通「输入→生成→预览→扣积分」，便于客户先看效果

### 页面完成与端到端验证（第二轮）
- software-engineer-2 完成 T04+T05：9 个页面全部重写为完整实现，24 个配套组件，build 通过；中途 npm install 损坏 @mui/icons-material 后已自修复
- 主理人（我）用 playwright-core + Edge 亲自端到端验证：注册（演示模式任意邮箱+密码≥8位+任意邀请码）→ 自动登录跳 /me（98/100 积分）→ /generate 填需求 → 生成 → /generating/{jobId} 4 阶段进度 → iframe 预览真实可交互应用（单词卡形态，认识/不认识/打乱顺序）→ 显示"本次消耗 1 积分 / 6.4k token / 剩余 97" → /me 积分看板与流水正确 → 零控制台报错
- 首个验证轮次页面卡"加载中…"是 vite 依赖重新预构建（new dependencies optimized → reload）的瞬态，重跑即正常；交付前需注意等 vite 稳定
- 遗留小问题：dev 模式下偶发 PAGEERROR "Cannot read properties of null (reading 'useContext')" ×2（疑似 HMR 瞬态/React 双实例），不影响功能，生产 build 待观察
- 演示模式兑换码：SC50 / SC200 / SC500（mockStore）
- 清单：项目根目录有大量 _*.log/_*.mjs 调试残留（工程师修复包依赖时产生 + 我的探针文件），打包部署前应清理

### 产品定位重大修正（客户反馈）
- 客户：做应用只是一方面，老师更多用教案/PPT/课件生成；生成内容要有 3D 效果帮学生理解；最大特点不是做应用而是方便老师办公（生成文档/课件）；要能跟上新教材（如今年初三数学换新版）
- 三项澄清拍板：3D 双形态（伪3D动画+真Three.js 3D）；输出网页+PPTX+Word且支持修改；教材知识用"AI联网搜+用户说明年份/版本/出版社+生成后教师核对+可补写/上传电子版"机制（多窗口级联选择）
- 许清楚重写 PRD 为 v2（覆盖 docs/PRD.md）：定位=备课办公台，主轴=教案/PPT/3D课件/文档，应用降为标签，新增教材版本机制/多格式导出/文档在线编辑；提示词工程扩为备课文档类+教材版本感知+3D课件三块；新增4张UI线框
- 高见远评估架构：不推翻，补强 T06（文档/课件生成，apps 表加 category app/doc，DocModel JSON 存 Storage + CDN 渲染）、T07（教材版本机制，Edge Function 搜索适配器+上传沉淀）、T08（导出 pptxgenjs/docx 结构映射 + TipTap 在线编辑，共用 DocModel 源）；3D 用 three 懒加载。3 条阻塞 P0：文档产物路径、PPTX/Word 保真度取舍、教材检索成本归属（均偏技术，可由我方推荐决策）

### 本机环境备忘
- Bash 工具的 shim 有故障（dirname/cd 报错），`cd` 不可用；PowerShell 的 `Start-Process npm.cmd` 被安全策略拦截
- **可用做法**：Bash 直接跑 `node.exe node_modules/vite/bin/vite.js`（默认 cwd 即工作区），或 PowerShell 直接调 node.exe 跑脚本
- 项目 package.json 含 `"type":"module"`，临时脚本必须用 `.cjs` 后缀，否则 require 报错
- Edge 路径：C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe
- 开发服务器：http://127.0.0.1:5173/

### 三阻塞 P0 决策已采纳 + T06 开工（客户拍板后）
- 客户在 AskUserQuestion 中选「接受三项推荐 + 直接开工 T06-T08」
- ① 文档网页产物走新 `/d/` 路径（不与应用 `/a/` 混放），doc_json 存 Storage（不进 DB 列）；② 导出用结构化映射（pptxgenjs/docx）；③ 教材检索走 Edge 后台、费用并入当次生成积分
- 已派 software-engineer-3（后台）实现 T06：文档/课件生成 + 3D 查看器 + 首页/生成入口改造（含 0012 迁移、0016 提示词 seed、_shared/doc/*、generate doc 分支、serve-app /d/ 分支、docService、DocRunPage、ThreeViewer、HomePage/GeneratePage 改造、mock 模式扩展）。T07/T08 在其后顺序推进

### T06 首次实现失败 + 文档补齐
- software-engineer-3 跑了 57m58s 后被 429 限流中断（"使用量已超出频率限制，2026-09-12 15:43:19 UTC+8 重置"），**没有产出任何 T06 代码**，仅占用了时间窗口。教训：长任务需更小批次；后续 T06-T08 必须切小批重试，避开限流窗口
- 客户追问进度并要求补 MRD/BRD。趁限流窗口由主理人直接落盘四份文档：
  - docs/MRD.md（v1）—— 9 节：市场概述/目标市场/竞争分析/市场机会/定位/商业模式/增长策略/风险/待确认
  - docs/BRD.md（v1）—— 10 节：业务背景/目标/利益相关方/功能&非功能需求/成功指标/约束假设/风险/组织/Roadmap/待确认
  - docs/DEPLOY.md（v1）—— 海外版 30 分钟上线流程（Supabase 新加坡 + GitHub Pages + DeepSeek，零现金）
  - docs/DEPLOY_CN.md（v1）—— 国内版迁移清单（ICP 备案 + 阿里云 OSS+CDN + DeepSeek 国内/豆包双适配 + 国家智慧教育平台检索）
- 限流 15:43 重置后重试 T06，建议切分：T06-A 后端（迁移+generate 分支+docService+serve-app/d/） → T06-B 前端（HomePage/GeneratePage/DocRunPage/ThreeViewer） → T07 → T08，每批 < 60min
- **对客户的反思**：上次工程师失败时主理人未第一时间告知，是协作失误；后续必须在 worker 失败后立刻同步给客户

### 客户第二轮拍板（商业决策）+ 四份文档同步更新
野马反馈六条：① 学校发展=本校先行再 3–5 所（不设硬性时间表）② 推广=主攻抖音，小红书不做 ③ 定价=太便宜要涨，但**教师档不变** ④ 迁移=等客户多了再迁、**数据跟着迁** ⑤ 新需求=**数据存储 + skill 沉淀降本** ⑥ T06 等限流过了（15:43）再试 T06-A

已据此更新：
- **MRD**：定价表重构（教师档 29/199 不变引流；教研组 999→1680 上调 68%；新增学校版 6800/19800/定制三档，摊到人均 99–136 元/年低于个人卡）；冷启动改「本校先行」；内容营销改抖音优先；待确认标记已定
- **BRD**：新增 **BR-014 内容数据存储** + **BR-015 Skill 自动提炼与复用降本**，并新增 **4.3 节**详述四步闭环（生成即沉淀 → 授权复用 → Skill 提炼 → 命中复用）与降本测算（命中可省 75%，按 40% 命中率整体降本约 30%）；Roadmap 拆 T06-A/T06-B，并把「生成即沉淀」埋进 T06（成本极低）
- **DEPLOY_CN**：时间表从固定 T+30/T+40 改为**按需触发**（付费>500 或 月生成>1万）；三条原则：备案 T+0 早办（免费但需 7–20 天）、数据跟着迁不是重建、海外版保留 7 天兜底

**关键洞察**：客户提的"Skill 降本"与"涨价保利润"是**两条并行路径**——涨价提收入、Skill 复用降成本，双管齐下才真正撑得起利润空间。这条要在后续方案里反复强调。

T06-A 待 15:43 限流重置后启动；已规划顺序：T06-A 后端 → T06-B 前端 → T07 → T08 → T09(Skill)，每批 < 60min

### T06-A 自动任务已建 + HY3 模型（待确认语义）
- 野马回复「需要的 模型改成 HY3 自动跑」→ 已建**一次性自动任务** `T06-A 文档课件生成后端（自动跑）`（id: d2088a67-0798-4759-b56e-ada73401a002），**2026-09-12 13:00** 触发，工作目录 F:\work buddy\数据\教师软件开发
- 任务内容严格限定 **T06-A 后端**：迁移 0012+0016(+0017 doc_library)、`_shared/doc/{types,validate,render,exportMap}.ts`、`_shared/three/scene.ts`、generate doc 分支、serve-app /d/ 分支、`src/types/doc.ts`、`docService`、generateService 扩展、装 three、mock 扩展；明确 /a/ 分支不动、积分逻辑只复用不改、three 动态 import
- 已要求**每完成一个子任务阶段性 commit**，避免长任务中断全丢（吸取 429 教训）
- ⚠️ **HY3 语义已确认（2026-09-12）**：野马明确指 **① 开发干活用的模型**切到 HY3（绕开 429 限流），**不是** ② 师创项目接入的 AI 模型。**因此不要给项目加 HY3 适配器**，项目的主力模型仍是 DeepSeek（+备份）
- 执行方式：由野马在 UI 侧把模型切到 HY3，13:00 自动任务即以此为模型执行；若自动任务未继承该模型仍撞 429，则改期 15:43 后重跑
- 兜底：若 13:00 仍撞 429，则改期到 15:43 限流重置后重跑
