# 2026-09-12

## 启动「师创」项目：教师 AI 教学应用生成平台（复刻飞象老师）

用本机 Edge + playwright-core 实地调研了 feixianglaoshi.com（agent-browser 的 Chromium 下载在国内超时，改用系统 Edge 驱动，可行）。

### 客户已拍板的决策
- 复刻范围：整个平台（AI 生成器 + 应用广场社区），AI 是重中之重
- 端形态：网页 + PWA 先行（零成本、发链接即用），架构预留原生 APP / 小程序
- 数据存储：云端多端同步（Supabase 免费层）
- 平台名：**师创**（代号 shichuang）
- 登录：**邮箱 + 密码 + 邀请码**（零成本零资质；不做手机短信、不做微信）
- 大模型：多家适配器，客户自己申请 Key、自己充值
- 商业模式：**会员/套餐 + 积分，线下人工充值，不做在线支付** → 兑换码与管理员后台是核心
- 部署：先用免费海外跑通，成功后迁国内；国内方案（阿里云 OSS+CDN / 腾讯云 COS+CDN）要提前备好迁移清单
- 积分定价暂缓：客户要先看清「做一次多少钱」再定价 → 成本统计升为首要功能

### 团队产出
- 许清楚 PRD：docs/PRD.md（560 行，P0 需求池 A~G、提示词工程专项、6 张 UI 线框、12 个待确认问题）
- 高见远架构：docs/ARCHITECTURE.md（1100 行，5 个任务约 130 文件）+ 2 张时序图 + 类图
  - 关键决策①：应用 HTML 永不过 Supabase 出网（免费层仅 10GB），走静态托管 + CDN，路径 /a/{年}/{月}/{id}/v1.html
  - 关键决策②：积分解耦 + 预扣退还用 Postgres 行锁 + 唯一索引幂等
  - 关键决策③：提示词存表分节版本化，system 固定前缀逐字节稳定以命中上下文缓存
- 寇豆码编码：T01/T02/T03 完成（基础设施、11 个迁移 + RLS + RPC、3 个 Edge Functions、4 家模型适配器 + mock、提示词 seed 16KB）；T04/T05 页面**仍是 18~33 行占位页**时遭遇 429 限流中断（运行 199 分钟）
- 第二轮工程师（software-engineer-2）已启动补 T04/T05 页面，并要求实现**演示模式**：无 Supabase 配置、无 API Key 时也能完整走通「输入→生成→预览→扣积分」，便于客户先看效果

### 页面完成与端到端验证（第二轮）
- software-engineer-2 完成 T04+T05：9 个页面全部重写为完整实现，24 个配套组件，build 通过；中途 npm install 损坏 @mui/icons-material 后已自修复
- 主理人（我）用 playwright-core + Edge 亲自端到端验证：注册（演示模式任意邮箱+密码≥8位+任意邀请码）→ 自动登录跳 /me（98/100 积分）→ /generate 填需求 → 生成 → /generating/{jobId} 4 阶段进度 → iframe 预览真实可交互应用（单词卡形态，认识/不认识/打乱顺序）→ 显示"本次消耗 1 积分 / 6.4k token / 剩余 97" → /me 积分看板与流水正确 → 零控制台报错
- 首个验证轮次页面卡"加载中…"是 vite 依赖重新预构建（new dependencies optimized → reload）的瞬态，重跑即正常；交付前需注意等 vite 稳定
- 遗留小问题：dev 模式下偶发 PAGEERROR "Cannot read properties of null (reading 'useContext')" ×2（疑似 HMR 瞬态/React 双实例），不影响功能，生产 build 待观察
- 演示模式兑换码：SC50 / SC200 / SC500（mockStore）
- 清单：项目根目录有大量 _*.log/_*.mjs 调试残留（工程师修复包依赖时产生 + 我的探针文件），打包部署前应清理

### 产品定位重大修正（客户反馈）
- 客户：做应用只是一方面，老师更多用教案/PPT/课件生成；生成内容要有 3D 效果帮学生理解；最大特点不是做应用而是方便老师办公（生成文档/课件）；要能跟上新教材（如今年初三数学换新版）
- 三项澄清拍板：3D 双形态（伪3D动画+真Three.js 3D）；输出网页+PPTX+Word且支持修改；教材知识用"AI联网搜+用户说明年份/版本/出版社+生成后教师核对+可补写/上传电子版"机制（多窗口级联选择）
- 许清楚重写 PRD 为 v2（覆盖 docs/PRD.md）：定位=备课办公台，主轴=教案/PPT/3D课件/文档，应用降为标签，新增教材版本机制/多格式导出/文档在线编辑；提示词工程扩为备课文档类+教材版本感知+3D课件三块；新增4张UI线框
- 高见远评估架构：不推翻，补强 T06（文档/课件生成，apps 表加 category app/doc，DocModel JSON 存 Storage + CDN 渲染）、T07（教材版本机制，Edge Function 搜索适配器+上传沉淀）、T08（导出 pptxgenjs/docx 结构映射 + TipTap 在线编辑，共用 DocModel 源）；3D 用 three 懒加载。3 条阻塞 P0：文档产物路径、PPTX/Word 保真度取舍、教材检索成本归属（均偏技术，可由我方推荐决策）

### 本机环境备忘
- Bash 工具的 shim 有故障（dirname/cd 报错），`cd` 不可用；PowerShell 的 `Start-Process npm.cmd` 被安全策略拦截
- **可用做法**：Bash 直接跑 `node.exe node_modules/vite/bin/vite.js`（默认 cwd 即工作区），或 PowerShell 直接调 node.exe 跑脚本
- 项目 package.json 含 `"type":"module"`，临时脚本必须用 `.cjs` 后缀，否则 require 报错
- Edge 路径：C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe
- 开发服务器：http://127.0.0.1:5173/

### 三阻塞 P0 决策已采纳 + T06 开工（客户拍板后）
- 客户在 AskUserQuestion 中选「接受三项推荐 + 直接开工 T06-T08」
- ① 文档网页产物走新 `/d/` 路径（不与应用 `/a/` 混放），doc_json 存 Storage（不进 DB 列）；② 导出用结构化映射（pptxgenjs/docx）；③ 教材检索走 Edge 后台、费用并入当次生成积分
- 已派 software-engineer-3（后台）实现 T06：文档/课件生成 + 3D 查看器 + 首页/生成入口改造（含 0012 迁移、0016 提示词 seed、_shared/doc/*、generate doc 分支、serve-app /d/ 分支、docService、DocRunPage、ThreeViewer、HomePage/GeneratePage 改造、mock 模式扩展）。T07/T08 在其后顺序推进

### T06 首次实现失败 + 文档补齐
- software-engineer-3 跑了 57m58s 后被 429 限流中断（"使用量已超出频率限制，2026-09-12 15:43:19 UTC+8 重置"），**没有产出任何 T06 代码**，仅占用了时间窗口。教训：长任务需更小批次；后续 T06-T08 必须切小批重试，避开限流窗口
- 客户追问进度并要求补 MRD/BRD。趁限流窗口由主理人直接落盘四份文档：
  - docs/MRD.md（v1）—— 9 节：市场概述/目标市场/竞争分析/市场机会/定位/商业模式/增长策略/风险/待确认
  - docs/BRD.md（v1）—— 10 节：业务背景/目标/利益相关方/功能&非功能需求/成功指标/约束假设/风险/组织/Roadmap/待确认
  - docs/DEPLOY.md（v1）—— 海外版 30 分钟上线流程（Supabase 新加坡 + GitHub Pages + DeepSeek，零现金）
  - docs/DEPLOY_CN.md（v1）—— 国内版迁移清单（ICP 备案 + 阿里云 OSS+CDN + DeepSeek 国内/豆包双适配 + 国家智慧教育平台检索）
- 限流 15:43 重置后重试 T06，建议切分：T06-A 后端（迁移+generate 分支+docService+serve-app/d/） → T06-B 前端（HomePage/GeneratePage/DocRunPage/ThreeViewer） → T07 → T08，每批 < 60min
- **对客户的反思**：上次工程师失败时主理人未第一时间告知，是协作失误；后续必须在 worker 失败后立刻同步给客户

### 客户第二轮拍板（商业决策）+ 四份文档同步更新
野马反馈六条：① 学校发展=本校先行再 3–5 所（不设硬性时间表）② 推广=主攻抖音，小红书不做 ③ 定价=太便宜要涨，但**教师档不变** ④ 迁移=等客户多了再迁、**数据跟着迁** ⑤ 新需求=**数据存储 + skill 沉淀降本** ⑥ T06 等限流过了（15:43）再试 T06-A

已据此更新：
- **MRD**：定价表重构（教师档 29/199 不变引流；教研组 999→1680 上调 68%；新增学校版 6800/19800/定制三档，摊到人均 99–136 元/年低于个人卡）；冷启动改「本校先行」；内容营销改抖音优先；待确认标记已定
- **BRD**：新增 **BR-014 内容数据存储** + **BR-015 Skill 自动提炼与复用降本**，并新增 **4.3 节**详述四步闭环（生成即沉淀 → 授权复用 → Skill 提炼 → 命中复用）与降本测算（命中可省 75%，按 40% 命中率整体降本约 30%）；Roadmap 拆 T06-A/T06-B，并把「生成即沉淀」埋进 T06（成本极低）
- **DEPLOY_CN**：时间表从固定 T+30/T+40 改为**按需触发**（付费>500 或 月生成>1万）；三条原则：备案 T+0 早办（免费但需 7–20 天）、数据跟着迁不是重建、海外版保留 7 天兜底

**关键洞察**：客户提的"Skill 降本"与"涨价保利润"是**两条并行路径**——涨价提收入、Skill 复用降成本，双管齐下才真正撑得起利润空间。这条要在后续方案里反复强调。

T06-A 待 15:43 限流重置后启动；已规划顺序：T06-A 后端 → T06-B 前端 → T07 → T08 → T09(Skill)，每批 < 60min

### T06-A 自动任务已建 + HY3 模型（待确认语义）
- 野马回复「需要的 模型改成 HY3 自动跑」→ 已建**一次性自动任务** `T06-A 文档课件生成后端（自动跑）`（id: d2088a67-0798-4759-b56e-ada73401a002），**2026-09-12 13:00** 触发，工作目录 F:\work buddy\数据\教师软件开发
- 任务内容严格限定 **T06-A 后端**：迁移 0012+0016(+0017 doc_library)、`_shared/doc/{types,validate,render,exportMap}.ts`、`_shared/three/scene.ts`、generate doc 分支、serve-app /d/ 分支、`src/types/doc.ts`、`docService`、generateService 扩展、装 three、mock 扩展；明确 /a/ 分支不动、积分逻辑只复用不改、three 动态 import
- 已要求**每完成一个子任务阶段性 commit**，避免长任务中断全丢（吸取 429 教训）
- ⚠️ **HY3 语义已确认（2026-09-12）**：野马明确指 **① 开发干活用的模型**切到 HY3（绕开 429 限流），**不是** ② 师创项目接入的 AI 模型。**因此不要给项目加 HY3 适配器**，项目的主力模型仍是 DeepSeek（+备份）
- 执行方式：由野马在 UI 侧把模型切到 HY3，13:00 自动任务即以此为模型执行；若自动任务未继承该模型仍撞 429，则改期 15:43 后重跑
- 兜底：若 13:00 仍撞 429，则改期到 15:43 限流重置后重跑

### 定价成本利润测算（docs/PRICING_PROFIT.md）
- 客户要求：出利润明细 + 按硅基流动定价 + 查清"买 token 半价"政策
- 联网查证（2026-08-17/08-23 官方公告 + 2026-09-01 第三方实测）：**DeepSeek 峰谷定价**，空闲=高峰半价，**周末全天+节假日全天算空闲**；硅基流动 9/1 起也改峰谷但谷段仅 02:00-08:00
- **核心发现**：三家单价相同（峰¥3/¥9、谷¥1.5/¥4.5），差别只在**半价窗口宽度**（DeepSeek≈79% / 百炼42% / 硅基流动25%）。教师晚上+周末备课 → **DeepSeek 天然半价**；硅基流动半价时段教师在睡觉，用不上 → **主力改 DeepSeek 官方，硅基流动降为备份/多模型源**（颠覆客户原"按硅基流动来"的预设，已说明）
- 测算结果：每积分成本 ¥0.030（基准）；毛利率 教师年卡55%/教研组64-73%/学校版64-74%；**最坏全高峰仍28%不亏**
- ⚠️ 重要：DeepSeek 8/17 是**涨价**（输出 ¥2/M→高峰¥9/M），BRD 里用的旧价 ~¥8/M 已过时，后续测算一律以 PRICING_PROFIT.md 为准

### T06-A 后端完成（自动任务 d2088a67 执行，commit 93d798e）
- **前提发现**：T06-A 主体在前序会话已实现（0012/0016、doc/{types,validate,render,exportMap}.ts、three/scene.ts、generate doc 分支、serve-app /d/ 分支、src/types/doc.ts、docService、mock 扩展、three@0.186 已装）。本次补齐 + 验收 + 首次建库提交。
- **本次新增**：`0017_doc_library.sql`（沉淀库 + RLS + `deposit_doc_library` 幂等函数）；generate doc 分支成功后写沉淀（失败 catch 吞掉，不影响主流程）；`deriveKeywords()` 从 DocModel 推导关键词
- **修掉 3 个真问题**：
  1. `_shared/doc/validate.ts` 模板字符串内含 ``` 反引号提前终止 → **整个 generate Edge Function 语法错误、无法部署**。tsc 不覆盖 supabase/ 所以一直没被发现
  2. ppt 类型 `blocks: []` 被 validateDoc 误判非法（ppt 真相源是 slides）→ Edge+前端两套校验器同步放宽
  3. PWA 预缓存把 three(747KB) 打进 SW → 教师首访即静默下载 747KB。改 `globIgnores` + CacheFirst 运行时缓存，预缓存 1818KB → **1070KB**
- **验收**：tsc 零错误；build 通过；31 个 Edge TS 语法全通过；Mock 冒烟 12/12（5 类前端样例 + 5 类 Edge 降级样例 + 2 负例拦截）
- **首次 git 初始化**（此前项目无 .git）：193 文件，hash `93d798efc9b9f3fa36d11430b977a343e9ecebdd`。.gitignore 补 `/_*`、`.pkg-repair/`、`.rollup-repair/`（排掉数百个历史 .tgz）

### 本轮回话收尾：主页微调 + 启动 T06-B
- 野马截图反馈：把「做互动应用」和「大家都在用」两个板块往前挪。已调整 `src/pages/HomePage.tsx`：顺序改为 Hero → 数据背书条 → 做互动应用 → 大家都在用 → 核心亮点 → 备课入口 → 底部 CTA。typecheck 通过，commit `cf99b5e`
- T06-A 后端经复核确实已在盘（`93d798e`），对齐架构、类型检查通过
- 已启动 T06-B 前端工程师（software-engineer-6），目标：GeneratePage 支持 doc 分支、新建 DocRunPage、新建 ThreeViewer（动态 import three）、注册路由、mock 链路跑通。要求分块提交、60 分钟内、避开 429
- **T06-B 完成（commit `2e49ad6`）**：工程师核对发现 DocRunPage/ThreeViewer/GeneratePage doc 分支/router `d/:id`/docService·generateService doc 分支/mock 文档链路/three globIgnores 其实已在 `93d798e` 整段实现；本次补齐 DocRunPage 版本号徽标 + 保存新版本/重新生成按钮、`docService.saveVersion` 在 MOCK 下真正落库递增版本。typecheck + build 通过（1151 模块，three 独立 chunk 746KB 未进主包，SW 预缓存已 globIgnores 排除 three）。**mock 默认开启**，本地无 Key 可跑通 输入→生成中→DocRunPage 预览 全链路
- **T06（A+B）整体可用**：文档生成主链路在 mock 下端到端跑通。剩余留给 T07（教材版本 TextbookCascade 现为 UI 壳）/ T08（导出 pptx/docx + 文档在线编辑）/ saveVersion 真实 Storage 写入
- **T07 完成（限流窗口刷新后重跑成功，software-engineer-8）**：迁移 0013/0014/0015（textbook_versions/textbook_knowledge/RLS）、Edge 搜索适配器（Tavily，密钥仅 Edge Secrets）、generate 新增 textbook_search 阶段（mock 优雅降级）、前端 textbookService/useTextbook/mockStore 种子、TextbookCascade 级联接线、VerifyBanner 红色核对横幅、GeneratePage/DocRunPage 接线、useGenerate 修 textbook_search 仅绑定版本时插入。提交 3265c29/3d9bf88/c9e2a06/3e236b8/4394d2b/fa8b53e。Edge esbuild 体检 34 文件通过；typecheck+build 通过。RLS 未真实库实测（本环境无 DB）

### 本轮：清旧定时任务 + 复核 T06-A + 主页改版（haoyue01.cn 风）
- 客户本轮回话要求：① 清除旧的 T06-A 自动任务 d2088a67（已删除）② 继续跑 T06-A ③ 主页做成 www.haoyue01.cn 的样子
- 发现：T06-A 实则已在上一轮（自动任务 d2088a67 触发后）实现并提交 `93d798e`。本轮回派 software-engineer-4 复核 = 代码已在盘、对齐 ARCHITECTURE、tsc 通过，**无新代码**（93d798e 即既有首 commit），无重复劳动
- 主页改版（software-engineer-5）：重写 `src/pages/HomePage.tsx`，借鉴 haoyue01.cn（实为「皓月运动会」办赛台）的清新校园风——Hero 大标题「备课，交给 AI」+ 数据背书条 + 01–05 亮点卡片 + 保留四备课入口 + 底部 CTA；`npm run build`/`typecheck` 通过，commit `a60dc33`
- 预览：开发服务器 http://localhost:5173/ 已起（后台 task ocjixv）
- 遗留：① 主页数据背书条数值为占位（待补真实统计，代码注释与 UI 小字已标注）② T06-B 其余前端（生成页/文档运行页/3D查看器）仍待做 ③ 旧定时任务已清，无需新建

### T07 + T08 完成（限流刷新后顺序重跑成功）
- **T07（software-engineer-7 撞 429 失败后，限流 20:52 刷新，software-engineer-8 重跑）**：迁移 0013/0014/0015（textbook_versions/textbook_knowledge/RLS）、Edge 搜索适配器 Tavily（密钥仅 `Deno.env.get` 不下发前端）、generate 新增 `textbook_search` 阶段（注入教材上下文、强制「⚠️ 待核对」、命中缓存跳过检索、SSE 事件）、前端 textbookService/useTextbook/mockStore 种子、TextbookCascade 级联接线（年级→学科→出版社→版本）、VerifyBanner 红色「本内容由 AI 生成，AI 可能编错，请教师核对」横幅、GeneratePage/DocRunPage 接线、useGenerate 修 textbook_search 仅「doc 且绑定版本」时插入（避免无版本卡 pending）。提交 3265c29/3d9bf88/c9e2a06/3e236b8/4394d2b/fa8b53e（fa8b53e 补真实分支 owner_id，表列 NOT NULL）。Edge esbuild 体检 34 文件通过；typecheck+build 通过。RLS 未真实库实测（本环境无 DB）
- **T08（software-engineer-9）**：浏览器端结构映射导出 PPTX/DOCX（exportService，`await import('pptxgenjs'|'docx')` 复用 exportMap 规则，非截图）；useDoc 读写封装 + uid 工具；TipTap 富文本 + 幻灯片编辑器（RichTextEditor/SlideEditor）；ExportMenu（导出 PPTX/Word/复制网页链接）；DocEditorPage(UI-5) 整合编辑区 + VerifyBanner + 上传修正 + ExportMenu，路由 /d/:id/edit；DocRunPage 加编辑入口。依赖安装成功（+93 包 pptxgenjs@^3.12.0、docx@^8.5.0、@tiptap/*@^2）。7 次分块提交 f21b164/6c0bd49/bb4c0e9/e3b43d4/f5a0c74/717eb4d/e70faec。动态 import 生效（RichTextEditor 379KB、pptxgen 384KB、three 747KB 均独立 chunk，不进首屏）。typecheck + build 通过
- **T06-T08 全流程 mock 下端到端可用**：输入→生成→/d/ 文档预览→保存版本→在线编辑→导出 PPTX/Word 全链路打通（本地无 Supabase Key 也能跑）
- **T08 遗留待确认（来自工程师报告）**：① 3D 在 PPTX 当前为静态预览页+备注「建议用网页版查看 3D」，是否本期渲染成图片嵌入；② 导出保真度（表格/图片转写为可读文本，未内嵌远程图片防 CORS）是否达标；③ 网页链接分享未做二维码（AppRunPage 已有 QrCodeDialog 可复用）；④ 真实模式 saveVersion 仍是占位（docService 真实分支返回 version=1、docJsonUrl=''），待接 Storage 写 docs/{id}/v{n+1}.json + publish；⑤ 编辑页 /d/:id/edit 设为公开，是否需仅作者可编辑
- **全局遗留**：① 主页数据背书条数值占位 ② RLS 真实 Supabase 库实测（本环境无 DB，待客户建好项目执行迁移 0013/14/15）

### 用户选「试跑预览」→ 进入「真正上线」准备
- 野马在 AskUserQuestion 选「试跑预览」，已给走查步骤并打开 http://localhost:5173/ 预览
- 续「下一步」= 推进「真正上线」（让老师用链接能打开）。新建 `docs/GO_LIVE_CHECKLIST.md`：零现金上线 6 步（建 Supabase ap-southeast-1 / 粘贴 0001-0017 SQL / 填 Key / 部署 generate+serve-app Edge Functions / 部署前端 GitHub Pages / 验证），含花费实话（月成本≈0或个位数元）与 3 条上线前必补代码坑（真实 saveVersion→Storage 为硬阻塞）
- 待客户：去 supabase.com 建项目（第1步）；我方并行补真实存档代码（不需其账号）

### 安全加固 + 仅作者可改（客户当晚要求，已先跑）
- 客户问「会不会被攻击/安不安全」+ 要求「编辑改成仅作者可改，否则不知怎么恢复」
- 安全现状核实：全表 RLS 已开；generate Edge 必须 `requireUser`(JWT)+扣积分（`check_generation_allowed` 日限/并发/月阀），**非登录不能白嫖算力**；API Key 仅 `Deno.env.get` 不下发前端；应用 iframe sandbox 无 allow-same-origin。主要缺口=编辑页前端未拦人
- 已实施（commit `7e1d08c`，typecheck 通过）：
  - 迁移 `0018_doc_storage_rls.sql`：建公开读 `docs` 桶 + `doc_object_owner()` 安全定义函数 + Storage 策略（仅作者增改删）
  - `docService.saveVersion` 真实分支：校验登录 + 比对 `apps.author_id`，仅作者可存新版本（写 Storage `docs` 桶 + 更新 apps 行）
  - 新增 `docService.isDocAuthor(docId)`
  - `DocRunPage`：「编辑/在线编辑/保存新版本」仅作者可见；`DocEditorPage`：非作者锁定只读（🔒 仅作者可编辑）
  - 顺手修真实模式 loadDoc 的 camel/snake 映射坑（doc_json_url 直读），否则真实模式打开文档会 404
- 结论：架构不算裸奔；仅作者可改后，陌生人改不了也删不了，作者本人随时能恢复/再编辑
- 仍待真实库实测：RLS/Storage 策略（本环境无 DB，客户建好项目跑 0001-0018 后实测）

### 客户新需求（积分调价/体验分过期/后台用户）→ 改为定时任务 9/14 0:01 执行
- 野马三点要求：① 文档积分消耗提高到 2–3 分/篇（当前 0012 seed：教案1/PPT2/2D课件2/3D课件3/办公文档1）② 注册赠送体验积分 7 天过期 ③ 后台能查看注册用户信息 + 防爬取防资料泄露
- 客户澄清：**整件事都在 9/14 0:01 由定时任务启动**，不是当下立刻做。已建一次性自动化 `273f27aa-ec41-49b5-bf02-fa915450732e`（2026-09-14 00:01，cwd 本项目，ACTIVE），prompt 含三项任务 + 收尾 typecheck/build/冒烟验证 + 本机 Bash shim 故障提示
- 本轮仅做摸底未改这些代码（文档类积分在 app_type_profiles.credit_cost；体验分发放位置待定时任务内定位，0006 的 free 套餐 credits=0 说明另有一处）
- ⚠️ 该定时任务只改**本地代码/迁移**，数据库迁移仍需客户在其 Supabase 库粘贴执行才生效
- 与「上线清单」互不冲突：客户 9/13 自建 Supabase（第1-5步），0:01 定时任务跑代码改动
